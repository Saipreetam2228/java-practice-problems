### 8. Inheritance Demo – Vehicle

- Base class: `Vehicle` with fields like `speed`, `capacity`.
- Subclass: `Car` with additional field `fuelType`.
- Methods to display details.
- Create and test both objects.
