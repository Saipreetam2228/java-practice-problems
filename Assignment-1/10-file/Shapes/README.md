### 10. Package & Interface

- Create a package `shapes` containing:
  - Interface `Shape` with `area()` and `perimeter()`.
  - Class `Circle` implementing `Shape`.
- In another file, import the package and test.
