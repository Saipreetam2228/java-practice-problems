### 5. Bank Account Simulation

- `BankAccount` class with:
  - Fields: `accountNumber`, `balance`.
  - Methods: `deposit()`, `withdraw()`, `displayBalance()`.
- Add validation for insufficient balance using **if-else**.
